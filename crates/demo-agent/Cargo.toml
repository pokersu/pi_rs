[package]
name = "demo-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Command-line demo agent with per-tool approval gate"
publish = false

[dependencies]
pi-agent = { path = "../pi-agent" }
pi-ai = { path = "../pi-ai" }
serde_json.workspace = true
tokio.workspace = true
